# dsh-plugin-task-notify bundle patch
# 将任务完成通知插件注册进 DSH 组合。
# Host 半负责桌面通知/提示音/HTTP 桥；Client 半负责应用内 toast 与设置 UI。
#
# 挂载方式见 README.md（profile package.json 的 link: 依赖 + dsh.profile.bundles）。
- insert:
    - id: task-notify
      name: 'dsh-plugin-task-notify'
